# 공통 작업 지침

- 설명과 작업 결과는 기본적으로 한국어로 작성한다.
- 커밋 메시지는 저장소에 별도 규칙이 없다면 `유형: 한국어 설명` 형식으로 작성한다. 예: `docs: 전역 작업 지침 수정`.
- 하나의 작업 단위를 완료하면 변경 내용과 필요한 검증 결과를 확인하고, 해당 작업의 변경만 바로 커밋한다.
- 푸시는 사용자가 요청할 때만 수행한다.

## 학습 중심 구현

- 구현 속도나 프로그램의 완성도보다 사용자가 코드와 기본 개념을 충분히 이해하는 것을 우선한다.
- 한 번의 구현은 가능하면 하나의 핵심 개념을 학습할 수 있는 작은 단계로 나눈다. 한 번의 대화에서 기능 전체를 완성할 필요는 없다.
- 각 단계에서 학습할 개념과 구현 이유를 설명하고, 사용하는 메서드·라이브러리의 역할과 동작 원리, 입력·출력 및 코드의 실행 흐름을 이해할 수 있도록 안내한다.
- 작은 실행 예시나 필요한 검증으로 해당 개념의 동작을 확인한다. 현재 단계에 대한 사용자의 질문과 이해를 우선하고, 다음 개념의 구현은 사용자의 진행 의사에 맞춘다.
- 요구사항을 충족하는 데 필요한 기능과 코드만 구현한다. 요구사항 밖의 기능·소스나 완성도를 높이기 위한 불필요한 추상화·최적화·확장 구조를 추가하지 않는다.
- 포트폴리오 표현을 위해 구현 범위를 넓히지 않고, 실제로 학습하고 구현한 내용을 문서에 반영한다.
- 작업 단위는 기능 전체가 아닌 하나의 학습 단계가 될 수 있으며, 해당 단계의 구현과 필요한 검증을 마치면 커밋한다.

## 구현 후 설명과 학습 기록

- 구현 후에는 무엇을 변경했고 어떤 요구사항을 해결했는지, 어떤 개념을 왜 사용했는지, 실제 코드의 어디에 적용했는지를 사용자에게 설명한다. 관련 파일과 코드 위치를 연결하고 핵심 코드만 짧게 발췌한다.
- 입력부터 출력까지의 실행 흐름과 직접 확인할 방법·예상 결과를 설명한다. 새 메서드·라이브러리는 역할, 주요 인자, 반환값과 동작 원리를 설명하고, 전문 용어는 처음 등장할 때 풀어 쓴다.
- 설명은 이번 단계의 핵심 개념에 집중한다. 문서 링크나 커밋 정보만으로 대화에서의 설명을 대신하지 않으며, 다음 단계는 사용자의 진행 의사에 맞춘다.
- 각 프로젝트의 학습 기록은 `docs/learning/`에 Markdown으로 축적한다. 실제 구현에서 다룬 하나의 개념을 하나의 파일로 정리하고, 같은 개념을 다시 다루면 기존 문서를 보완한다.
- 개념 문서의 파일명은 `001-csv-dict-reader.md`처럼 세 자리 순번과 영문 소문자 하이픈 형식의 개념명으로 작성한다. 순번은 최초 작성 순서로 부여하고 기존 파일명은 유지한다. 제목과 본문은 한국어로 작성한다.
- 개념 문서는 다음 공통 순서로 구성하되, 해당하지 않는 내용은 억지로 채우지 않는다.
  1. 학습 목적: 이번 요구사항과 이 개념이 필요한 이유
  2. 핵심 개념: 기본 원리와 사용한 메서드·라이브러리의 동작
  3. 프로젝트 적용: 관련 소스의 상대경로 링크, 함수·클래스 이름, 핵심 코드와 선택 이유
  4. 실행 흐름: 작은 입력 예시부터 출력까지의 과정
  5. 확인 방법과 결과: 실행 방법, 예상 결과, 실제 검증 결과와 검증 주체
  6. 질문과 추가 확인: 실제로 나온 질문, 확인한 답변과 아직 남은 의문
  7. 참고자료: 실제로 참고한 자료의 출처와 링크
- `docs/learning/README.md`를 학습 목록으로 사용하고, `순서 | 개념 | 적용한 기능 | 문서` 형식의 표를 유지한다. 첫 개념 문서를 작성할 때 목록도 만들고 이후 함께 갱신한다.
- 루트 README에는 `학습 기록` 섹션을 두어 학습 목록과 대표 개념 문서로 연결하고, 각 개념을 프로젝트의 어느 부분에 적용했는지 짧게 소개한다. 실제 문서가 생긴 뒤 유효한 상대경로로 연결한다.
- 학습 기록은 개념 설명과 실제 구현·검증 사실을 바탕으로 작성한다. AI가 설명하거나 실행한 내용을 사용자가 직접 이해·실험한 경험으로 표현하지 않는다. 사용자의 이해, 시행착오, 소감은 확인된 내용만 기록한다.
- 대화 원문을 복사해 보관하지 않고 개념과 근거를 정리한다. 문서 작성을 위해 요구사항 밖의 기능을 추가하지 않으며, 구현과 관련 학습 문서·목록 갱신을 같은 작업 단위로 검증하고 커밋한다.

## 과제 요구사항

- 과제 요구사항 PDF는 저장소 루트 기준 `docs/assignment-requirements.pdf`에 둔다.
- 과제 작업은 프로젝트 `AGENTS.md`에 정리된 요구사항을 기준으로 진행한다. 요구사항이 정리되지 않았거나 변경·누락·불명확한 부분이 있으면 `docs/assignment-requirements.pdf`를 확인한다.

## 디렉터리 구조

- 소스 코드는 각 애플리케이션의 전용 디렉터리에 둔다. 단일 애플리케이션은 기본적으로 루트의 `src/`를 사용하고, 여러 애플리케이션은 `apps/<앱 이름>/` 아래에 각각 구성한다.
- 애플리케이션은 언어가 아니라 독립적으로 실행·빌드·배포하는 단위로 구분한다. 각 애플리케이션의 내부 구조와 설정 파일 위치는 과제 요구사항과 언어·프레임워크 관례를 따른다.
- 테스트는 기본적으로 각 애플리케이션의 `tests/`에 두고, 여러 애플리케이션 간 통합 테스트는 루트의 `tests/`에 둔다. 프레임워크에 별도 관례가 있으면 이를 따른다.
- 문서는 루트의 `docs/`에 모으고, 참고자료는 `docs/references/`에 두어 출처와 활용 목적을 기록한다.
- 문서용 실행 화면은 `docs/images/screenshots/`, 구조도는 `docs/images/diagrams/`에 둔다. 프로그램이 사용하는 리소스는 해당 애플리케이션의 관례에 맞는 위치에 둔다.
- 사용 예제와 작은 샘플 입력은 `examples/`, 개발·검증용 보조 스크립트는 `scripts/`에 둔다.
- 필요한 디렉터리만 생성하고, 예상되는 확장만을 위해 애플리케이션을 미리 분리하거나 기존 구조를 정당한 이유 없이 재편하지 않는다.
- 빌드 결과·캐시·임시 파일은 버전 관리에서 제외한다.
- README에서 주요 디렉터리의 역할을 설명하고, 관련 문서와 이미지는 상대경로로 연결한다.

## README 관리

- README는 채용 담당자와 기술 면접관이 프로젝트의 가치와 본인의 기여를 빠르게 파악할 수 있도록 작성한다.
- 첫 부분에 프로젝트의 목적, 해결하려는 문제, 핵심 결과를 요약한다.
- 개발 기간, 개인·팀 프로젝트 여부, 본인의 역할과 구현 범위를 명시한다.
- 주요 기능과 사용 흐름을 설명하고, 이해에 도움이 되는 실행 예시·스크린샷·데모를 제공한다.
- 핵심 구조와 기술 선택의 이유를 설명한다. 기술을 단순히 나열하지 않고, 해결하려는 문제와 연결한다.
- 주요 기술적 문제에 대해 검토한 대안, 선택한 접근, 선택 이유와 한계를 설명한다.
- 테스트·실험·성능 측정 등으로 결과를 뒷받침하고, 수치에는 측정 환경과 조건을 명시한다.
- 다른 사람이 결과를 재현할 수 있도록 필요한 설치·실행·검증 방법을 안내한다.
- 현재 한계와 개선 과제를 완료된 기능과 구분한다.
- 과제 제공 코드·외부 자료·협업 결과와 본인의 기여를 구분하고, 확인되지 않은 성과나 경험을 만들어내지 않는다.
- 프로젝트 성격에 맞는 항목만 포함하고, 핵심 내용은 앞에, 상세 설명은 별도 문서에 두어 연결한다.
- 기능·사용 방법·검증 결과가 바뀌면 관련 README 내용도 함께 갱신한다.

## 커밋 메시지 유형

- `feat`: 새로운 기능 추가
- `fix`: 버그 수정
- `docs`: 문서 추가·수정
- `style`: 동작 변경 없는 코드 형식 수정(공백, 들여쓰기 등)
- `refactor`: 기능 변경이나 버그 수정 없는 코드 구조 개선
- `perf`: 성능 개선
- `test`: 테스트 추가·수정
- `build`: 빌드 시스템이나 의존성 변경
- `ci`: CI/CD 설정 및 스크립트 변경
- `chore`: 위 유형에 해당하지 않는 기타 유지보수 작업
- `revert`: 이전 커밋 되돌리기
